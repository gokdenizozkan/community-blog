<div>
  <h1><%= @tag.name %></h1>

  <% @tag.taggings.each do |tagging| %>
    <% unless tagging.article.published %>
      <div class="nes-container">
        <h2><%= link_to tagging.article.title, article_path(tagging.article) %></h2>
      </div>
    <% end %>
  <% end %>
</div>